function solve() {
    Array.from(document.querySelectorAll('button.add-product')).forEach(x => x.addEventListener('click', onClick));
    let result = document.querySelector('textarea');
    let products = document.querySelectorAll('.product');

    function onClick(event) {
        //TODO:
    }
}